'use client';

import { useState } from 'react';
import { Check, Clock3, Crown, History, X } from 'lucide-react';

type Asset = { id: string; generationJobId: string; provider: string; version: number; uri: string; mimeType: string; width: number; height: number; durationSeconds?: number; reviewStatus: 'pending' | 'approved' | 'rejected'; preferred?: boolean; createdAt: string; supersededAt?: string };
type Review = { id: string; status: string; notes?: string; rejectionReason?: string; continuityAssessment: string; reviewerActor: string; reviewedAt?: string; createdAt: string };
type Job = { id: string; status: string; provider: string; estimatedCost: number; actualCost: number; promptSnapshot: string; createdAt: string };
type Readiness = { ready: boolean; blockers: string[]; warnings: string[] };

export function MediaReviewPanel({ seriesId, episodeId, sceneId, shotId }: { seriesId: string; episodeId: string; sceneId: string; shotId: string }) {
  const base = `/api/series/${seriesId}/episodes/${episodeId}/scenes/${sceneId}/shots/${shotId}`;
  const [assets, setAssets] = useState<Asset[] | null>(null);
  const [jobs, setJobs] = useState<Job[]>([]);
  const [reviews, setReviews] = useState<Record<string, Review[]>>({});
  const [readiness, setReadiness] = useState<Readiness | null>(null);
  const [comparison, setComparison] = useState<Record<string, unknown> | null>(null);
  const [error, setError] = useState('');
  const [busy, setBusy] = useState('');

  async function load() { setBusy('load'); setError(''); try { const [assetResponse, jobResponse, readinessResponse] = await Promise.all([fetch(`${base}/assets`), fetch(`${base}/generation`), fetch(`${base}/readiness`)]); if (!assetResponse.ok || !jobResponse.ok || !readinessResponse.ok) throw new Error('Unable to load media review data.'); setAssets((await assetResponse.json()).data); setJobs((await jobResponse.json()).data); setReadiness((await readinessResponse.json()).data); } catch (cause) { setError(cause instanceof Error ? cause.message : 'Unable to load media review data.'); } finally { setBusy(''); } }
  async function action(asset: Asset, name: 'approve' | 'reject' | 'preferred') { setBusy(`${asset.id}:${name}`); setError(''); const payload = name === 'reject' ? { rejectionReason: 'Rejected during storyboard review.' } : { notes: 'Reviewed in storyboard workspace.' }; const response = await fetch(`${base}/assets/${asset.id}/${name}`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify(payload) }); if (!response.ok) setError((await response.json()).error?.message || 'Review action failed.'); await load(); setBusy(''); }
  async function history(asset: Asset) { const response = await fetch(`${base}/assets/${asset.id}/reviews`); if (response.ok) { const payload = await response.json() as { data: Review[] }; setReviews((current) => ({ ...current, [asset.id]: payload.data })); } else setError('Unable to load review history.'); }
  async function compare(asset: Asset) { const other = assets?.find((value) => value.id !== asset.id); if (!other) { setError('Create another version before comparing.'); return; } const response = await fetch(`${base}/assets/${asset.id}/compare`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ compareWith: other.id }) }); if (response.ok) setComparison((await response.json()).data); else setError('Unable to compare versions.'); }

  return <section className="mt-4 border border-stone-800 bg-stone-900 p-4" aria-labelledby={`media-review-${shotId}`}><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-[0.16em] text-amber-500">Media review</p><h3 id={`media-review-${shotId}`} className="mt-1 text-base font-semibold">Generated assets</h3></div><button type="button" onClick={load} disabled={busy === 'load'} className="rounded border border-stone-700 px-3 py-1.5 text-xs text-stone-200 disabled:opacity-50">{busy === 'load' ? 'Loading...' : 'Load review'}</button></div>{assets === null && <p className="mt-4 text-sm text-stone-500">Load generated assets and review history for this shot.</p>}{error && <p role="alert" className="mt-3 text-sm text-red-400">{error}</p>}{readiness && <div className={`mt-4 border-l-2 pl-3 text-xs ${readiness.ready ? 'border-emerald-500 text-stone-400' : 'border-red-500 text-red-300'}`}><p>{readiness.ready ? 'Continuity readiness clear for review.' : `Approval blocked: ${readiness.blockers.join(' ')}`}</p>{readiness.warnings.map((warning) => <p key={warning} className="mt-1 text-amber-300">Warning: {warning}</p>)}</div>}{assets?.length === 0 && <p className="mt-4 border border-dashed border-stone-700 p-5 text-sm text-stone-500">No generated assets yet. Prepare and complete a mock generation before review.</p>}{assets?.map((asset) => <article key={asset.id} className="mt-4 border border-stone-800 bg-stone-950 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><p className="font-semibold text-amber-300">Version {asset.version}</p>{asset.preferred && <span className="inline-flex items-center gap-1 bg-amber-900/40 px-2 py-1 text-[10px] text-amber-200"><Crown size={11} /> Preferred</span>}</div><p className="mt-1 text-xs text-stone-500">{asset.provider} · {asset.durationSeconds || 0}s · {asset.width}x{asset.height}</p><p className="mt-1 text-xs text-stone-500">Job {asset.generationJobId} · {new Date(asset.createdAt).toLocaleString()}</p></div><span className="border border-stone-700 px-2 py-1 text-xs text-stone-300">{asset.reviewStatus}</span></div><div className="mt-3 flex aspect-video items-center justify-center border border-dashed border-stone-700 bg-stone-900 text-xs text-stone-500">{asset.uri.startsWith('mock://') ? 'Mock media preview - metadata only' : asset.uri}</div><div className="mt-3 flex flex-wrap gap-2"><button type="button" disabled={!readiness?.ready || asset.reviewStatus !== 'pending' || !!busy} onClick={() => action(asset, 'approve')} className="inline-flex items-center gap-1 rounded bg-emerald-700 px-2 py-1.5 text-xs disabled:opacity-40"><Check size={13} /> Approve Asset</button><button type="button" disabled={asset.reviewStatus !== 'pending' || !!busy} onClick={() => action(asset, 'reject')} className="inline-flex items-center gap-1 rounded border border-red-800 px-2 py-1.5 text-xs text-red-300 disabled:opacity-40"><X size={13} /> Reject Asset</button><button type="button" disabled={asset.reviewStatus !== 'approved' || asset.preferred || !!busy} onClick={() => action(asset, 'preferred')} className="inline-flex items-center gap-1 rounded border border-amber-700 px-2 py-1.5 text-xs text-amber-200 disabled:opacity-40"><Crown size={13} /> Mark Preferred</button><button type="button" onClick={() => compare(asset)} className="rounded border border-stone-700 px-2 py-1.5 text-xs">Compare Versions</button><button type="button" onClick={() => history(asset)} className="inline-flex items-center gap-1 rounded border border-stone-700 px-2 py-1.5 text-xs"><History size={13} /> View History</button></div>{reviews[asset.id]?.map((review) => <div key={review.id} className="mt-3 border-l-2 border-stone-700 pl-3 text-xs text-stone-400"><p><Clock3 className="mr-1 inline" size={12} />{review.status} by {review.reviewerActor}</p>{review.notes && <p className="mt-1">{review.notes}</p>}{review.rejectionReason && <p className="mt-1 text-red-300">{review.rejectionReason}</p>}</div>)}</article>)}{comparison && <div role="dialog" aria-label="Asset version comparison" className="mt-4 border border-amber-800 bg-amber-950/20 p-4"><div className="flex justify-between"><h4 className="font-semibold text-amber-200">Version comparison</h4><button onClick={() => setComparison(null)} aria-label="Close comparison" className="text-stone-400">×</button></div><pre className="mt-3 overflow-auto text-xs text-stone-300">{JSON.stringify(comparison, null, 2)}</pre></div>}{jobs.length > 0 && <p className="mt-4 text-xs text-stone-500">Generation state: {jobs[0].status} · estimated cost {jobs[0].estimatedCost}</p>}</section>;
}
