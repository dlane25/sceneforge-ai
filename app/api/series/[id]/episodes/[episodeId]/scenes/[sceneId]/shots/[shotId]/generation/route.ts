import { NextResponse } from 'next/server'; import { requireUser } from '@/lib/auth'; import { apiError } from '@/lib/api'; import { generationService } from '@/lib/media';
export async function POST(_request: Request, { params }: { params: Promise<{ id: string; episodeId: string; sceneId: string; shotId: string }> }) { try { const { user } = await requireUser(); const p = await params; return NextResponse.json({ data: await generationService.prepare(user, p.id, p.episodeId, p.sceneId, p.shotId) }, { status: 201 }); } catch (error) { return apiError(error, 'GENERATION_PREPARE_FAILED', 'Unable to prepare generation'); } }
export async function GET(_request: Request, { params }: { params: Promise<{ id: string; episodeId: string; sceneId: string; shotId: string }> }) { try { const { user } = await requireUser(); const p = await params; return NextResponse.json({ data: await generationService.history(user, [p.id, p.episodeId, p.sceneId, p.shotId]) }); } catch (error) { return apiError(error, 'GENERATION_HISTORY_FAILED', 'Unable to load generation history'); } }
