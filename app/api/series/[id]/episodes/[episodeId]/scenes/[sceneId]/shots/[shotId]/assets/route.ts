import { NextResponse } from 'next/server'; import { requireUser } from '@/lib/auth'; import { apiError } from '@/lib/api'; import { mediaReviewService } from '@/lib/media';
export async function GET(_request: Request, { params }: { params: Promise<{ id: string; episodeId: string; sceneId: string; shotId: string }> }) { try { const { user } = await requireUser(); const p = await params; return NextResponse.json({ data: await mediaReviewService.getShotAssets(user, [p.id, p.episodeId, p.sceneId, p.shotId]) }); } catch (error) { return apiError(error, 'ASSETS_LIST_FAILED', 'Unable to list assets'); } }
